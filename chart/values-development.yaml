# Fixed credentials are intentionally scoped to the local development/E2E
# release. Never use this values file for a production deployment.
postgresql:
  auth:
    username: dsa
    password: development-password
    database: dsa
  storage:
    storageClassName: local-path

redis:
  auth:
    password: development-password
