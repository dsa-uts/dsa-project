# Results v3

Tool: built-in image_gen

Edit the supplied UI mockup with exactly one localized navigation change. Remove the entire 'Validation Results' topbar menu item and its underline. Keep the existing 'Results' item and make it active with bold white text and a short white underline directly below Results. Topbar left items must be exactly DSA, Dashboard, Results; right items remain Grading, Admin, Logout. Preserve every other element, all table data, Japanese text, search, badges, sidebar, pagination, colors, spacing, dimensions and layout unchanged. The deleted menu area becomes uninterrupted blue topbar background. No other changes.
